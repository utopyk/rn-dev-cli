{
  "name": "smoke-rn",
  "version": "0.1.0",
  "private": true,
  "description": "Smoke-test fixture — looks like an RN project to detectProjectRoot but ships no real Metro/build pipeline.",
  "main": "index.js",
  "dependencies": {
    "react-native": "0.74.0"
  }
}
