// Empty entry point. Daemon integration tests never boot Metro against this
// fixture in Phase 13.1 — it exists so the worktree path is a plausible
// React-Native-shaped directory. Later phases (13.2+) will extend this
// with a Metro config once the daemon actually owns session startup.
